//selecting userconst selectBtn = () => {    const gatherSelection = document.querySelectorAll('.selection');    gatherSelection.forEach(btn => {        btn.addEventListener('click',(e)=> {            const selected = e.target;            const selectedAttr = selected.getAttribute('alt');            console.log('this is what was click',selected);            console.log('this is your alt tag',selectedAttr);            if(selectedAttr === 'rock'){                const getUser = document.querySelector('.user');                getUser.innerHTML = "rock";                computerSelection();                userVsComputer();            }else if(selectedAttr === 'paper'){                const getUser = document.querySelector('.user');                getUser.innerHTML = "paper";                computerSelection();                userVsComputer();            }else if(selectedAttr === 'scissors'){                const getUser = document.querySelector('.user');                getUser.innerHTML = "scissors";                computerSelection();                userVsComputer();            }        })    });};selectBtn();//select computerconst computerSelection = () =>{    const getComputerSelection = document.querySelector('.computer');    const computerArray = ['rock','paper','scissors'];    getComputerSelection.innerHTML = computerArray[Math.floor(Math.random() * computerArray.length)];};//stack the winnersconst userVsComputer = () => {    const userValue = document.querySelector('.user').innerHTML;    const computerValue = document.querySelector('.computer').innerHTML;    const getWinner = document.querySelector('.winner');    if(userValue === computerValue){        getWinner.innerHTML = 'STALEMATE';            }else if(userValue === "rock" && computerValue === "scissors"){                getWinner.innerHTML = 'ROCK WINS!';            }else if(userValue === "scissors" && computerValue === "paper"){                getWinner.innerHTML = 'SCISSORS WINS!';            }else if(userValue === "paper" && computerValue === "rock"){                getWinner.innerHTML = 'PAPER WINS!';            }else if(userValue === "rock" && computerValue === "paper"){                getWinner.innerHTML = 'PAPER WINS!';            }else if(userValue === "paper" && computerValue === "scissors"){                getWinner.innerHTML = 'SCISSORS WINS!';            }else if(userValue === "scissors" && computerValue === "rock"){                getWinner.innerHTML = 'ROCK WINS!';            }        };//rock beats scissors, scissors beats paper, paper beats rock.